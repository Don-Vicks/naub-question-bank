{
  "name": "naub-question-bank",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo for NAUB Question Bank - Frontend and Pipeline",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.7.3"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "pnpm@9.0.0",
  "dependencies": {
    "next": "^16.2.10"
  }
}
